# Brain Changelog

High-level project + brain changes. **Not a code changelog** (see `git log` for that). This tracks:

- Architectural shifts (e.g. "migrated to X")
- New features shipped (with link to `features/<slug>.md`)
- Brain restructures (folder splits, doc rewrites)
- Decisions reversed
- External constraint changes (legal, vendor, deadline) sourced from `transcripts/` or `emails/`

## Conventions

- Newest entry on top
- Date format: `YYYY-MM-DD`
- One entry per change. Use the type tags: `feature` `bugfix` `refactor` `decision` `brain` `chore`
- Link out: `See .brain/features/<slug>.md`, `See .brain/transcripts/<file>.md`, `See PR #<n>`

## Entries

| Date | Type | Description |
|------|------|-------------|
| 2026-08-01 | bugfix | **state-integrity phase 7 — an independent cold review, and three things this branch got wrong about itself.** A reviewer with no context on the work audited both repos from source. (1) **`strictScope` was half a fix.** It narrowed the two strict rows to the shipping feature and left the other seven sweeping the whole brain, so one stray `<div>` in an unrelated legacy verification doc, one moved screenshot, or one malformed `plans/*/meta.json` still refused **every** ship in the repo — naming a file the shipper never touched. This is the third occurrence of "hardened one path, moved the hole", and the first two were in this same branch. Generalized to `opts.scope`, narrowing doc paths, dependency refs, verdicts, the raw-HTML ban, image links, and plans; `strictScope` kept as a read-compat alias. Deliberately NOT a `newFailuresAfter` diff — a dangling dependency on the feature being shipped is both pre-existing and disqualifying. (2) **The flagship gate was a green tick over the empty set.** With every shipped feature grandfathered, the strict rows evaluated nothing and reported `pass` with `0 shipped feature(s) proven` — printed by the very commit titled "promote strict from advisory to a gate". They now report **`skip`** with the debt count; `chrome.js`/`dashboard.js` treat `skip` as neither pass nor fail. (3) **Nine `brainCheck` rows and the atomic-write section had no discriminating fixture**, in the branch whose own rule says a validator without one is a claim. Proven by mutation: replacing `writeFileAtomic` with a plain `writeFileSync` left the suite fully green. Assertions 186 → 220, and the new atomicity cases assert what actually separates `rename(2)` from a truncating write — the inode changes, and a reader holding the file open still sees the whole old file. Also: `buildFeaturesTable` moved to `brain-data.js` so the suite stops hand-copying it, the zero-dep CI check counts keys instead of testing `{}` for truthiness, and `fetch-depth: 0` so receipt ancestry can resolve in CI at all. |
| 2026-07-31 | bugfix | **state-integrity phase 6 — the ratchet was an off switch, and regen shipped on one path again.** A fifth adversarial review dropped the score, correctly. (1) `policy.strict_grandfathered` had NO integrity check, so adding any slug exempted it — new work could ship with zero verification by listing itself. It is now validated against the ON-DISK list (never the projection, or a ship could exempt itself in the same breath): every entry must be a known feature and already `shipped`. This immediately caught a real bug in the template's own list, which carried a `cut` feature — and falsified the previous commit message's claim that the list "names the eight features that shipped before the invariant". (2) Index regeneration was wired into `ship` only, so `features set-status` took a brain from green to red while exiting 0 — the identical "hardened one path, moved the hole" mistake the ship gate itself made two phases earlier. Regen now follows every status write, and `ship` re-runs `brainCheck` AFTER the write so "no drift left behind" is a checked claim rather than a comment; the markerless and missing-index paths now fail loudly instead of exiting 0 silently. (3) An UNCLOSED HTML comment hid a verdict from every renderer while parsing as PASS — the closed case was fixed and the unclosed one never considered, exactly as had already happened with fences. (4) Receipt commits must now be hex object ids: `HEAD`, a branch, or a tag resolved through git while binding the verdict to nothing. Assertions 168 to 186. |
| 2026-07-31 | bugfix | **state-integrity phase 5 — the ship gate was deadlocked, and the comment fix defeated itself.** A fourth adversarial review found two HIGH defects, both introduced by phase 4. (1) `brain ship` was refused in EVERY brain that had a `features/index.md`: the drift check ran against the projected state, but the index is generated FROM the tracker, so on disk it still (correctly) said `in-progress`. The only escape was hand-editing the index to a premature `shipped` — a gate satisfiable only by lying. The index check is now skipped in projection mode (it describes disk, not a proposal) and `ship` regenerates the index after a successful write. Nothing caught this because no fixture had ever shipped through a brain with an index; `makeBrain` now writes one by default. (2) The HTML-comment strip used a negative lookahead for `brain:verification` that scanned the REST OF THE DOCUMENT, so any doc containing a receipt kept every earlier comment unstripped — the invisible-verdict spoof worked precisely on docs that satisfy the strict gate — and a verdict inside the receipt body scored too. Receipts are now removed first, then all remaining comments, then the verdict is read. Also: a MISSING index.md with tracked features now fails (deleting the file was the cheapest permanent silence), and `features index --write --create` scaffolds one. Assertions 156 to 168, including an end-to-end ship through an indexed brain. |
| 2026-07-31 | bugfix | **state-integrity phase 4 — the strict gate was unusable, and two more spoofs.** A third adversarial review found that `--strict`, being always-on at the ship path and scoped to the WHOLE brain, meant one legacy feature predating the invariant refused **every** future ship in both repos — the flagship gate was decorative exactly where it lives. `brainCheck` gained `opts.strictScope`: ambient `brain check --strict` still audits the whole brain (that is the report), while both ship paths scope the proof requirement to the feature being shipped (that is the gate). Two new verdict spoofs closed: a fence closed by MORE backticks than it opened (CommonMark closes on `>=`, the `\1` backreference required exact length) and a verdict inside an HTML comment (renders as nothing, parsed as real — the `brain:verification` receipt is itself a comment, so it is preserved by negative lookahead). Assertions 149 to 156, including a fence/comment matrix asserting the parser never scores a verdict a renderer would hide. |
| 2026-07-31 | feature | **state-integrity phase 3 — receipts, telemetry, and the clone case.** Verdicts are now bound to code: a `brain:verification` receipt names the commit a verdict was taken against, and `--strict` (plus both ship paths, always) requires that commit to exist **and** be an ancestor of HEAD — a PASS taken on a branch that never landed describes code that is not what shipped. Outside a git repo, presence is the ceiling and provenance is reported unverifiable rather than failing. `brain ship` became transactional across its two files: the feature-list bytes are snapshotted and restored if the checkpoint write throws (a *missing* `progress.md` still only warns — `appendProgressEntry` returns null by design so a brain without a cursor can still ship). New `brain metrics` over an append-only `runs/gates.jsonl` written by every `brain verify`: first-pass rate, per-check failure counts, p50/p95 duration, and it names any check that has never failed in 3+ runs. New `brain init --state-only` for repos cloned from a template — wipes inherited `features/`, `runs/`, `plans/`, `screenshots/`, `evals/`, keeps `rules/`, `recipes/`, `codebase/`, `high-level-architecture/`, `HARNESS.md`, `verify.json`. Assertions 124 to 149, with provenance fixtures built against a throwaway git repo (orphan commit, unknown sha, missing receipt). |
| 2026-07-31 | feature | **state-integrity phase 2 — closing what adversarial review found.** Two independent reviewers audited phase 1; between them they found one P0 and several P1s, three of which were regressions phase 1 introduced. `features set-status --status shipped` was a complete bypass of the new ship gate (hardening `ship` alone moved the hole rather than closing it) — it now runs the same projection preflight; transitions *out* of a state stay ungated so broken records remain repairable. `oneInProgressEnforced()` is now shared, because the setter and checker had drifted apart a second time in the opposite direction (absent policy: setter said not-enforced, checker said enforced). Verdict spoofs closed: 4-space indented code blocks, unclosed fences, and a trailing contradicting emoji each scored a clean PASS; the token must now lead its value and identical restated verdicts are accepted rather than called ambiguous. Drift check fixed for one false positive (prose with a pipe) and four false negatives (capitalized status, two status words in a row, first-match-only attribution, features absent from the index). `loadFeatureList` hard-fails on structure only — full-field failure had locked `features set-status` out of repairing the very records it exists to fix. New: **`brain check --strict`** requires a PASS verification for every shipped feature — opt-in ambiently for read-compat, always enforced at both ship paths. Assertions 78 → 124. |
| 2026-07-31 | decision | **`brain ship` is now preflight-then-commit.** It previously wrote `feature_list.json` and appended the progress checkpoint, THEN ran `brainCheck`, then exited 1 with the flip already on disk — specified behavior (`docs/REVIEW-ARCHITECTURE.md`), reasoned as reporting the failure honestly. Reversed: reporting a failure honestly does not stop later reads from seeing a feature marked `shipped` on a brain that never passed its checks. `brainCheck` gained `opts.list` so a projection is validated before any write; on failure nothing is written and the previous status stands. See `.brain/features/state-integrity/state-integrity.md`. |
| 2026-07-31 | feature | **state-integrity (feat-008), phase 1.** New `lib/state.js` — the first non-`review/` module in `lib/` and the shared owner of the feature-list schema, the single verdict parser, and `writeFileAtomic`. `brain check`'s first row became real structural validation (`{}`, `[]`, `"hello"`, `42`, duplicate ids/slugs, unknown status, shipped-without-evidence were all previously accepted as "feature_list.json parses"). ONE `parseVerdict` replaces two that disagreed — the old one decided purely by emoji substring, so `**Verdict**: PASS` read as `unknown` on every display surface while `brainCheck`'s presence-only regex passed it. New checks: readable verdict, verification image links resolve. `brainCheck` now honors `policy.one_in_progress_at_a_time` instead of hardcoding it (it disagreed with `features set-status`). New rule 7 `rules/state.md`. |
| 2026-07-31 | chore | **First CI, and the repo's first automated tests.** `scripts/check-state-invariants.mjs` — 78 assertions over synthetic brains in a temp dir, every case a shape that used to pass silently — registered in `.brain/verify.json` and run by a new `.github/workflows/ci.yml` (`brain verify --stage baseline`, Node pinned, zero-dep invariant asserted). `skill --check` was called "intended for CI" in four places with nothing running it. No test framework added: plain node scripts + the check registry, per `codebase/programming-model.md`. `bootstrap` stage now has declared checks (it was a silent 0-match no-op). |
| 2026-07-30 | brain | Vendored the AXI standard from [kunchenguid/axi](https://github.com/kunchenguid/axi) (MIT, © 2026 Kun Chen) into the repo as the installable `brain-axi` skill (`.claude/skills/brain-axi/SKILL.md`); attribution in `THIRD-PARTY-NOTICES.md`. `HARNESS.md`, `rules/toon-axi.md`, and `CLAUDE.md` previously pointed at a global-only `axi` skill that no cloner had; all three now resolve in-repo. Not covered by `brain skill --check` — it is upstream's doc, not a mirror of our CLI surface, so re-sync manually against upstream. |
| 2026-07-23 | feature | harness-mode: brain-axi becomes a full 5-subsystem harness — `brain verify` (declared check registry `.brain/verify.json`, stages bootstrap/baseline/verify), `start`/`done` bookend playbooks, `brain init` scaffolder. Replaces bespoke init.sh/verify scripts in target repos; `setup --commands` generation deferred (plan harness-parity D0). See `.brain/features/harness-mode/harness-mode.md`. |
| 2026-07-14 | brain | Turned `.brain` onto brain-axi itself — replaced base-template placeholders with real harness (HARNESS, codebase/programming-model, 4 layer rules, architecture, feat-001 core-cli, feat-002 brain-review). See `.brain/HARNESS.md`. |
| 2026-07-13 | feature | First iteration committed (c1b0880). |
